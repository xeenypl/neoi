# neoi
This is repo of my current system configuration.
# Way you don't wanna use this:
- I use workman keyboard layout (with my own mods mainly Polish characters).
- I use vim (neovim) with many custom remaping (like `s` to `v`).

